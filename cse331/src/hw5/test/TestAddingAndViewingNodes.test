# Tests that a graph can be created, a single node added and
# a second node added. Then tests by adding the same node
# (no change to the graph). View at each stage.

# Create a graph
CreateGraph graph1

# Print the nodes in the graph
ListNodes graph1

# Add a node
AddNode graph1 n1

# Print the nodes in the graph
ListNodes graph1

# Add another node
AddNode graph1 n2

# Print the nodes in the graph
ListNodes graph1

# Add the same node as before
AddNode graph1 n2

# Print the nodes in the graph
ListNodes graph1